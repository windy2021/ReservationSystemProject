@model ReservationSystemProject.Areas.Admin.Models.Employee.Details

@{
    ViewData["Title"] = "Details";
}

<div class="views">
    <h1 class="mt-3 mb-2">Details</h1>

    <div>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.FullName)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.FullName)</dd>
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.MobileNumber)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.MobileNumber)</dd>
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Email)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Email)</dd>
            <dt class="col-sm-2">@Html.DisplayNameFor(model => model.Role)</dt>
            <dd class="col-sm-10">@Html.DisplayFor(model => model.Role)</dd>
        </dl>
    </div>
    <div class="form-group col-12 d-flex justify-content-start">
        <a asp-action="Update" asp-route-id="@Model.Id" asp-route-role="@Model.Role" class="btn btn-primary col-2 align-self-center me-1">Update</a>
        <a asp-action="Index" class="btn btn-primary col-2 align-self-center ms-1">Back to List</a>
    </div>
</div>
